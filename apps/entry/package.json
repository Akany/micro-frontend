{
  "name": "entry",
  "version": "1.0.0",
  "description": "Entry proxy service",
  "main": "./dist/index.js",
  "private": true,
  "scripts": {
    "start": "node ./dist/index.js",
    "build": "esbuild index.ts --outdir=./dist --bundle --platform=node"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.19.5",
    "express": "^4.18.2",
    "express-http-proxy": "^2.0.0"
  }
}
